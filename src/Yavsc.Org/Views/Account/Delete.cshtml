@model UnregisterViewModel
@{
    ViewBag.Title = "Supprimer mon compte";
}

<h2>@ViewBag.Title</h2>

<div class="row">
    <div class="col-md-8">
        <p>
            Cette action est <strong>immédiate et irréversible</strong>. Toutes les données
            associées à votre compte seront supprimées, et vous serez déconnecté.
        </p>

        <form asp-controller="Account" asp-action="Delete" method="post" class="form-horizontal" role="form">
            @Html.Hidden("UserId")
            <button type="submit" class="btn btn-danger">Confirmer la suppression</button>
            <a asp-controller="Home" asp-action="Index" class="btn btn-default">Annuler</a>
        </form>
    </div>
</div>